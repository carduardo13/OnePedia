generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model pirate {
  id         String @id @default(auto()) @map("_id") @db.ObjectId
  color      String
  imageUrl   String
  namePirate String
}
